name: AI-Agent-Check
on: [push]
jobs:
  test-in-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Build and Run
        run: |
          docker build -t my-agent .
          docker run --rm my-agent python agent.py
